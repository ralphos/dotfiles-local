# Use better bind key
unbind C-s
set -g prefix C-a

# Split window automatically to 25%
unbind-key C-t
bind-key C-t split-window -l 10

# Split window horizontally to 65%
unbind-key C-y
bind-key C-y split-window -h -l 20
